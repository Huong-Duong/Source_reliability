[project]
name = "source-reliability"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Huong-Duong", email = "192438257+Huong-Duong@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
source-reliability = "source_reliability:main"
run-app = "source_reliability.main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
